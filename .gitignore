# Virtuelle Umgebung (nie ins Git – ist groß und maschinenspezifisch)
.venv/
venv/

# Python-Cache
__pycache__/
*.pyc

# Lokale Vektor-Datenbank (wird aus den Dokumenten neu erzeugt)
chroma_db/

# Graph-Extraktions-Checkpoints (werden lokal aus den Dokumenten neu erzeugt)
graph_checkpoints/

# Eingespeiste Dokumente: die PDFs werden bewusst mitversioniert (Portfolio-Demo).

# Reproduzierbarer Lock kann rein, lokale Logs nicht
*.log
.DS_Store

# Claude Code lokale Laufzeit-Dateien
.claude/
